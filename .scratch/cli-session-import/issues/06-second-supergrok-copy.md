# 06: Second SuperGrok is Add account, then switch

**What to build:** Tell the user with two SuperGrok memberships (different MLIDs) to Add account and switch. Do not run two live official logins. Existing snapshot switcher is the product.

**Blocked by:** None (can start immediately).

**Status:** done (copy on CHANGELOG / account UI; no dual auth.json)

- [x] No second concurrent `auth.json`
- [x] Copy points at Add account / switch
- [x] Out of scope for the session-import PR if it starts growing auth code
